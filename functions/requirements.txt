google-cloud-firestore==2.*
google-cloud-storage==2.*
firebase_functions~=0.1.0
firebase-admin>=6.0.0 # Specify a version for stability
sentence-transformers>=2.2.0 # Specify a version
torch>=1.9.0 # Add torch explicitly
scikit-learn>=1.0.0 # Specify versions
pandas>=1.3.0
numpy>=1.20.0
scipy>=1.7.0
requests>=2.25.0
# hf_xet # Keep only if you specifically use XetHub integration
# flask==2.* # firebase_functions includes Flask, might be redundant
openai # Keep if used for neutralization
# dotenv # Primarily for local dev, usually not needed in deployment
beautifulsoup4>=4.9.0
newspaper3k>=0.2.8
lxml[html_clean]>=4.6.0